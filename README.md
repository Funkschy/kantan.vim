# Vim config for Kantan

## Description
Just a really primitive config, to allow vim users to use Kantan with a bit more comfort.

## Installation
### Plug

Add `Plug 'funkschy/kantan.vim'` to your .vimrc and execute `:PlugInstall`
